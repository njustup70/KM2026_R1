# 移植新框架的改动说明

## 关于vscode配置

新框架使用 clangd，禁用了 Microsoft C/C++ (cpptools) 插件。如果VS Code里之前没有装 clangd，装上

## 删除原cube中配置的线程 

新框架使用 `Reactor46H_Initialize();` 接管了freertos，所以cube里把之前的线程全部删掉（默认的线程删不掉，随便改个名字，栈大小设成128）

## 私有库说明

新框架将部分库作为战队的私有库，需`ReactorLibs`目录下手动clone战队的私有库，链接如下：
https://github.com/njustup70/Mods_70.git

## 融合框架
### CMake

在CubeMX生成的CMakeLists.txt中的最后一行，加上如下代码：
```
include(ReactorLibs/Reactor46H.cmake)
```
### Main函数
- 在`main.c`的顶端加上包含`"std_cpp.h"`
```
/* Private includes ----------------------------------------------------------*/
/* USER CODE BEGIN Includes */
#include "std_cpp.h"
/* USER CODE END Includes */
```
- 在`main.c`的`MX_FREERTOS_Init()`前加上`Reactor46H_Initialize()`
```
MX_USART3_UART_Init();
MX_USART6_UART_Init();
MX_TIM1_Init();
MX_USART1_UART_Init();
/* USER CODE BEGIN 2 */
    Reactor46H_Initialize();
/* USER CODE END 2 */

/* Call init function for freertos objects (in cmsis_os2.c) */
MX_FREERTOS_Init();
```
---

2026.3.21
在框架进行更新后，只需要run一下`reactorlize.py`脚本即可融合框架

---
### 手动导入Extensions70库
在`ReactorLibs`目录下手动clone`Extensions70`库

### 无法找到 Freertos 下的 GCC port.c 的问题

报错信息如下：
```
[cmake] CMake Error at cmake/stm32cubemx/CMakeLists.txt:106 (target_sources):
[cmake]   Cannot find source file:
[cmake] 
[cmake]     E:/up70/up70_2026/KungFuMaster_R1_V2/Middlewares/Third_Party/FreeRTOS/Source/portable/GCC/ARM_CM4F/port.c
[cmake] 
[cmake] 
[cmake] CMake Error at cmake/stm32cubemx/CMakeLists.txt:105 (add_library):
[cmake]   No SOURCES given to target: FreeRTOS
```
原因是没有在 cube 里生成 `CMake`，导致缺少 `Default Compiler/Linker GCC`
因此重新生成 CMake 工具链，并勾选`Copy all used libraries into ...` 即可

### keil 编译时找不到'portmacro.h'
大致报错内容如下
```
'portmacro.h' file not found
```
同样，是因为没在 cube 里重新生成 MDK-ARM 工具链，生成即可

### keil 编译时 typeid 报错
打开`Options for target`，在`C/C++(AC6)`选项中，找到`Misc Controls`，添加 `-frtti`

全称：Run-Time Type Information（运行时类型信息），作用是开启 C++ 运行时类型识别功能，让程序能在运行时知道对象的实际类型。

